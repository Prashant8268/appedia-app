<link rel="stylesheet" href="/css/chats_section.css">
<div class="header">
    <!-- <img src="https://cdn-icons-png.flaticon.com/512/134/134808.png" alt="Chats"> -->
    <span>Chats</span>
    <div class="new-chat "   id="open-new-chat">
        <img src="https://cdn-icons-png.flaticon.com/512/10322/10322417.png" alt="">
        <div class="chat-user-list hide">
            <% for(user1 of users) { %>
            <li class="open-chat" user_Email="<%= user1.email %>"><%= user1.name %></li>
            <% } %>
        </div>
    </div>
    <div class="line"></div>
</div>
<div class="container">  
    <div class="chats-container" id="chats-container">
        <% for(let chat of chats){ %>  
            <% if (locals.user.id == chat.sender.id) { %>
            <li class="open-chat" user_Email="<%= chat.receiver.email %>">
                <div>
                    <img src=<%= chat.receiver.avatar %> alt="Profile">
                    <div>
                        <div><%= chat.receiver.name %></div>
                        <div>
                            <% if (chat.lastSender==locals.user.id) { %>
                                You : 
                            <% } %>
                            <span style="font-weight: bold;"><%= chat.latestMessage %></span> 
                        </div>
                    </div> 
                </div>
            </li>
            <% } else { %>
            <li  class="open-chat" user_Email="<%= chat.sender.email %>">
                <div>
                    <img src=<%= chat.sender.avatar %> alt="Profile">
                    <div>
                        <div><%= chat.sender.name %></div>
                        <div>
                            <% if (chat.lastSender==locals.user.id) { %>
                                You : 
                                <% } %>
                            <span style="font-weight: bold;"><%= chat.latestMessage %></span> 
                        </div>
                    </div> 
                </div>
            </li>
            <% } %>
        <% } %>
        
        </div> 

    <div class="chats hide">
        <div class="single-chat">
        </div>
        <form action="" class="send-form" id="send-form">
            <input type="text" id="msg">
            <input type="button" value="Send"  id="send-message">
        </form>

    </div>

</div>

<script src="https://cdn.socket.io/4.5.4/socket.io.min.js"></script>
<script src="/js/chats_section.js" type="text/javascript"></script>
